# Tools

Operationally useful scripts live here so the project root stays focused on
runtime modules.

- `research/`: offline analysis, backtests, gate audits, and parameter review.
- `diagnostics/`: focused one-off diagnostics for external systems.

Run these from the project root so their default CSV paths resolve to the live
workspace files.
